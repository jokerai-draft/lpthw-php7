#
关于浮点数

浮点数
- 浮点数的样子
- 浮点数的不规则样子
- 浮点数的赋值 会将浮点数变成 浮点数规则样子 // 这是正常使用的浮点数。特殊情况特殊区分

将不规则浮点数转化为规则浮点数
(何时会发生? 系统内会发生，大部分时候会发生，也就是 不规则浮点数一旦参与下一步运算 会立刻死掉)(何时不想让它发生，立刻保护起来 as 显式保护无效零位 变为字符串格式)

从规则浮点数获取字符串
- 从规则浮点数获取字符串

从不规则浮点数获取字符串
- 从不规则浮点数获取字符串



类型戏法
https://www.php.net/manual/zh/function.floatval.php
